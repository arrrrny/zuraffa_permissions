name: zuraffa_permissions_platform_interface
description: The platform interface contract for zuraffa_permissions — MethodChannel protocol + PermissionPort bridge.
version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
  zuraffa_permissions: ^1.0.0

dependency_overrides:
  zuraffa_permissions:
    path: ../zuraffa_permissions

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - permissions
  - platform-interface
  - zuraffa

repository: https://github.com/arrrrny/zuraffa_permissions
issue_tracker: https://github.com/arrrrny/zuraffa_permissions/issues
